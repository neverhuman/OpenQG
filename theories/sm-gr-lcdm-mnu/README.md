# SM + GR + LambdaCDM + massive neutrinos

This is the benchmark baseline.

The adapter is intentionally trivial in v0.1:

- predictions are read from committed smoke fixtures
- the manifest exposes named, physical parameters
- the theory is the comparison reference, not a new proposal

