# OpenQG V6.1 — critical reviews from ChatGPT (via jailgun/jailhard)
Generated 2026-06-11T06:01Z. Bundle: V6.1 source + audit-cascade docs + real V6-campaign
run-data (white papers, ledgers, 754-call funnel). --task-file prompt; contract honored exactly.

## Headline verdict (review 01)
"V6.1 is a go for a V7 engineering campaign, no-go for external scientific claims. The archive
supports: 'the current oracle rejects the known V4/V5/V6 reward hacks.' That is useful — it is
not yet modified cosmology. V7 should measure success by how many exploits it makes impossible,
not by whether a new high score appears."

## On the survivor (review 07)
"ndgp-proposed-fixed-rc is the least-dead body in the morgue, not a live lead: derivation-rich,
data-disfavored (delta_lnz = −1.69), surviving on non-data credit — and structurally incomplete
(bound nDGP with no brane term; the StructurallyUngenerated check covers w0/alpha but not
bound MG families). Keep it as a regression fixture, not a candidate."

## V7 top risks (reviews 01-05, 10)
1. Anchor calibration is a constant offset — residual bias off-anchor is the most dangerous
   remaining physics surface (V6 exploits lived in d(lA)/dh).
2. StructurallyUngenerated matches term-name SUBSTRINGS and skips bound MG families.
3. Occam n_eff = record count, not effective modes under covariance (laplace_log_evidence
   exists in scoring/evidence.rs and is unused by the campaign path).
4. Oracle-repair teaches the model the oracle's numerical surface.
5. Reproducibility: review bundles must carry full ledgers + fixtures + machine-generated
   rescore artifacts (the 25.0 rescore was not independently derivable from the bundle).
