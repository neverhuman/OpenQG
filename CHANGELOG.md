# Changelog

## Unreleased

- OpenQG benchmark and control-plane scaffold established
- contract specs now live under `contracts/specs/` with generated JSON outputs under `contracts/generated/schemas/`
- `just check` is the composite validation lane and `just release-check` builds on `check`, `score`, and `release-pack`
- proof routing, generated zones, and release evidence lanes are in place
- the repository still has known audit gaps that are being reduced iteratively
