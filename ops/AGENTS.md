# Ops Agent Notes

The `ops/` tree owns security policy and workflow-oriented control files.

- keep shell wrappers like `tools/security-lane.sh` under the ops-owned lane surface
- use `just security` after ops changes
