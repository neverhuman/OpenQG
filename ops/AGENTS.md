# Ops Agent Notes

The `ops/` tree owns runbooks, security policy, and workflow-oriented control files.

- keep ZYAL runbooks under `ops/zyal/`
- keep shell wrappers like `tools/security-lane.sh` under the ops-owned lane surface
- use `just security` and `just zyal-validate` after ops changes

