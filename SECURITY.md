# Security

OpenQG treats the following as sensitive:

- secrets and API keys
- raw upstream data not explicitly committed as fixtures
- generated locks and score artifacts
- any candidate theory that depends on non-public inputs

Report security issues privately through the host platform.

Do not add credentials, tokens, or private data to the repository.

